# Prefer the real flags from the MSVC CMake configure (compile_commands.json
# lives under build/<preset>/; symlink or copy to the source root if needed).
CompileFlags:
  CompilationDatabase: build/msvc-ninja
